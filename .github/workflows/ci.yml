# The COMMON legs (lint/gate + build & test on macOS + Linux, debug/release, coverage) route through
# the shared reusable workflow. The package-UNIQUE jobs fan out alongside it: the Foundation-free-core
# Linux portability build, an API-stability check, the cross-platform compile matrix, a path-gated
# ASan/TSan pass over the unsafe scanner + concurrency layer, the libFuzzer pass, the ordo-one
# benchmark suite, the DocC link check, and the DocC Pages deploy.
#
# AemiJSON's test suite fetches conformance/benchmark fixtures via `swift package fetch-fixtures`
# (network) before `swift test`. RESOLVED: the reusable workflow now takes a `fixtures-command` input
# and the `quality:` job passes the fetch-fixtures command, so the reusable's macOS/Linux build/test
# legs materialize fixtures before testing (no fixture-dependent test fails/skips on them). The
# fixture-fetching benchmark/fuzz jobs below keep their own fetch step (they run outside the reusable).
# The opt-in AemiJSONNIO test
# (`AEMIJSON_NIO=1 swift test --filter AemiJSONNIOTests`) and the `coverage-check --floor` gate that the
# old bespoke build-test ran are likewise not represented in the reusable.
name: CI

on:
  push:
    branches: [main]
    paths-ignore: ["README.md", "CONTRIBUTING.md", "ROADMAP.md", ".gitignore", ".githooks/**"]
  pull_request:
    paths-ignore: ["README.md", "CONTRIBUTING.md", "ROADMAP.md", ".gitignore", ".githooks/**"]
  # On-demand trigger for the slow sanitizer passes (and a full platform/docs run when wanted), and to
  # (re)generate the benchmark baseline on this runner class via the `update_baseline` input below.
  workflow_dispatch:
    inputs:
      update_baseline:
        description: "Regenerate the benchmark baseline (.benchmarkBaselines/main) and upload it as an artifact to commit"
        type: boolean
        default: false

permissions:
  contents: read

# Cancel superseded runs on branches/PRs; never cancel an in-progress run on main.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

# Type-check-budget calibration for the manifest's env-tunable -warn-long-* flags on the BESPOKE
# jobs below (the reusable swift-quality workflow exports its own copy for the common legs):
# 250 ms absorbs shared-runner wall-time noise; local builds keep the strict 100 ms default.
env:
  AD_TYPECHECK_BUDGET_MS: "250"

jobs:
  quality:
    # Shared build/test and quality checks resolve the published Aemi dependency.
    # Additional jobs below cover platform builds, fuzzing, benchmarks, and Pages.
    uses: Aemi-Studio/aemi/.github/workflows/swift-quality.yml@main
    with:
      # arcleak and dolly GATE (arcleak measured 0 findings family-wide; dolly runs the token
      # pass against .dolly-baseline.json, so only NEW clones fail). deadwood is report-only —
      # its syntax-mode dataflow cannot see writes through closures or mutating methods.
      enable-static-analysis: true
      dev-env-var: AEMIJSON_DEV
      enable-sanitizers: true
      sanitizers: "address thread"
      enable-coverage: true
      # The Linux release-mode test leg segfaults on process EXIT (all tests pass first) under the
      # dated nightly-6.4.x Linux container — a nightly-toolchain teardown crash, not an AemiJSON bug
      # (macOS release + Linux debug are clean; the parse/decode fan-out is structured concurrency and
      # the buffer pool holds managed arrays). Advisory until a stable 6.4 Linux toolchain ships,
      # matching the standalone `linux` job below; the Linux DEBUG leg stays required.
      linux-release-advisory: true
      # The conformance/benchmark suites need fixtures fetched (network) before `swift test`; the
      # reusable build/test legs run this first so fixture-dependent tests don't fail/skip on them.
      fixtures-command: "swift package --allow-network-connections all --allow-writing-to-package-directory fetch-fixtures"

  changes:
    name: Detect unsafe / concurrency changes
    needs: quality
    # Path gate for the sanitizer passes. Two risk surfaces warrant the (otherwise main-only)
    # ASan/TSan runs on a PR: the unsafe raw-pointer scanner (`Sources/AemiJSONCore/Core/**`) for
    # OOB/UAF under ASan, and the shared-state concurrency layer — the encoder buffer pool and the
    # concurrent decoder — under TSan. The latter lives in `Sources/AemiJSON/Concurrency/**` and
    # `Sources/AemiJSON/Codable/**`, so a PR touching either must trip the sanitizers too. On push the
    # filter compares against the prior commit; on a PR, against the merge base.
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      core: ${{ steps.filter.outputs.core }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            core:
              - 'Sources/AemiJSONCore/Core/**'
              - 'Sources/AemiJSON/Concurrency/**'
              - 'Sources/AemiJSON/Codable/**'

  linux:
    name: Build & test (Linux, Foundation-free core)
    needs: quality
    # AemiJSONCore is Foundation-free; building it in isolation on Linux guards portability for
    # zero-dependency consumers — a check the reusable's whole-package Linux leg doesn't make. The
    # full package is exercised by the reusable test-linux leg.
    #
    # Advisory (continue-on-error) for now: the package targets Swift 6.4, whose stable Linux toolchain
    # has not yet shipped, so this runs a dated 6.4 nightly Linux container, which can be unstable
    # independent of the code. Promote to a required gate once the matching stable Linux toolchain ships.
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    # Dated nightly pins the toolchain for reproducible CI; bump deliberately like a dependency (#14).
    container: swiftlang/swift:nightly-6.4.x-noble@sha256:54970846d48a8255eee5986aa5bbd259b7f73bbf38429cc2428ce6a62a390b47
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - name: Show toolchain
        run: swift --version
      - name: Build Foundation-free core
        run: swift build --target AemiJSONCore

  api-stability:
    name: API breaking changes
    needs: quality
    # Catch accidental public-API removals/changes in PRs (additions are fine). Compares the PR head
    # against the PR's base commit on main.
    if: github.event_name == 'pull_request'
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Select latest Xcode
        run: sudo xcode-select -s "$(find /Applications -maxdepth 1 -name 'Xcode_*.app' -print | sort -V | tail -n 1)"
      - name: Install Swift 6.4 dev toolchain (no hosted macOS image ships 6.4)
        uses: Aemi-Studio/aemi/.github/actions/setup-swift@main
      - name: Diagnose API breaking changes vs base
        run: swift package diagnose-api-breaking-changes "${{ github.event.pull_request.base.sha }}"

  platforms:
    name: Build (${{ matrix.platform }})
    needs: quality
    # Cross-platform compile is broad but costly: run it on main / on demand, not on every PR
    # (the macOS leg in the reusable `quality` job already covers compilation for PRs).
    if: github.event_name != 'pull_request'
    runs-on: macos-26
    # Use the installed SwiftPM for manifest resolution; Xcode embeds an older SwiftPM.
    strategy:
      fail-fast: false
      matrix:
        include:
          - { platform: iOS, sdk: iphoneos, triple: arm64-apple-ios18.0 }
          - { platform: tvOS, sdk: appletvos, triple: arm64-apple-tvos18.0 }
          - { platform: "watchOS (arm64_32)", sdk: watchos, triple: arm64_32-apple-watchos11.0 }
          - { platform: "watchOS (arm64)", sdk: watchos, triple: arm64-apple-watchos11.0 }
          - { platform: visionOS, sdk: xros, triple: arm64-apple-xros2.0 }
    steps:
      - uses: actions/checkout@v6
      - name: Select latest Xcode
        run: sudo xcode-select -s "$(find /Applications -maxdepth 1 -name 'Xcode_*.app' -print | sort -V | tail -n 1)"
      - name: Install Swift 6.4 dev toolchain (no hosted macOS image ships 6.4)
        uses: Aemi-Studio/aemi/.github/actions/setup-swift@main
      - name: Cache SwiftPM platform build
        uses: actions/cache@v5
        with:
          path: |
            ~/Library/Caches/org.swift.swiftpm
            .build
          key: platform-spm-${{ runner.os }}-${{ matrix.platform }}-${{ env.TOOLCHAINS }}-${{ hashFiles('Package.swift') }}-${{ hashFiles('Sources/**') }}
          restore-keys: |
            platform-spm-${{ runner.os }}-${{ matrix.platform }}-${{ env.TOOLCHAINS }}-${{ hashFiles('Package.swift') }}-
            platform-spm-${{ runner.os }}-${{ matrix.platform }}-${{ env.TOOLCHAINS }}-
      - name: Build for ${{ matrix.platform }}
        run: |
          sdk_path="$(xcrun --sdk '${{ matrix.sdk }}' --show-sdk-path)"
          swift build --sdk "$sdk_path" --triple '${{ matrix.triple }}' --target AemiJSON

  sanitizers:
    name: Sanitize (${{ matrix.name }})
    needs: [quality, changes]
    # Sanitizers are slow (TSan ~5-15x, ASan ~2x) and rebuild the whole graph under instrumentation,
    # so run them on main / on demand — and, additionally, on any PR that touches the unsafe scanner
    # OR the concurrency layer (`Sources/AemiJSONCore/Core/**`, `Sources/AemiJSON/Concurrency/**`,
    # `Sources/AemiJSON/Codable/**` — gated by the `changes` job), where a memory or data-race bug is
    # most likely to be introduced. Other PRs skip them. The conformance + simdjson corpora double as
    # the stress input that trips data races (TSan) and OOB / use-after-free (ASan) in the unsafe
    # scanner and the shared-state concurrency layer. These find what `-enable-actor-data-race-checks`
    # (actor-isolation only) cannot. TSan and ASan are mutually exclusive, hence two passes.
    # Package-unique (not the reusable's sanitizer leg): path-gated, and each pass combines flags.
    if: github.event_name != 'pull_request' || needs.changes.outputs.core == 'true'
    runs-on: macos-26
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - { name: thread, flags: "--sanitize=thread" }
          - { name: address, flags: "--sanitize=address --sanitize=undefined" }
    steps:
      - uses: actions/checkout@v6
      - name: Select latest Xcode
        run: sudo xcode-select -s "$(find /Applications -maxdepth 1 -name 'Xcode_*.app' -print | sort -V | tail -n 1)"
      - name: Install Swift 6.4 dev toolchain (no hosted macOS image ships 6.4)
        uses: Aemi-Studio/aemi/.github/actions/setup-swift@main
      - name: Show toolchain
        run: swift --version
      - name: Cache SwiftPM
        uses: actions/cache@v5
        with:
          path: |
            .build
            ~/Library/Caches/org.swift.swiftpm
          key: spm-${{ runner.os }}-san-${{ matrix.name }}-${{ hashFiles('Package.swift') }}-${{ hashFiles('Sources/**', 'Tests/**', 'Plugins/**') }}
          restore-keys: |
            spm-${{ runner.os }}-san-${{ matrix.name }}-${{ hashFiles('Package.swift') }}-
            spm-${{ runner.os }}-san-${{ matrix.name }}-
      - name: Fetch conformance & benchmark fixtures
        run: swift package --allow-network-connections all --allow-writing-to-package-directory fetch-fixtures
      - name: Test under ${{ matrix.name }} sanitizer
        run: swift test ${{ matrix.flags }}

  fuzz:
    name: Fuzz (libFuzzer, Linux)
    needs: quality
    # libFuzzer (`-sanitize=fuzzer`) is a Linux capability of the Swift toolchain — the Darwin SDK
    # rejects the flag — so the gated `AemiJSONFuzz` target builds and runs here, time-boxed, on
    # main / manual dispatch. Advisory (nightly toolchain), like the Linux build job. A crash fails
    # the job and uploads the reproducer; commit any reproducer as a regression test.
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    # Dated nightly pins the toolchain for reproducible CI; bump deliberately like a dependency (#14).
    container: swiftlang/swift:nightly-6.4.x-noble@sha256:54970846d48a8255eee5986aa5bbd259b7f73bbf38429cc2428ce6a62a390b47
    continue-on-error: true
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - name: Show toolchain
        run: swift --version
      - name: Fetch fixtures (seed corpus)
        run: swift package --allow-network-connections all --allow-writing-to-package-directory fetch-fixtures
      - name: Build fuzz target
        env:
          AEMIJSON_FUZZ: "1"
        # Build the executable PRODUCT, not the target: `AemiJSONFuzz` is `-parse-as-library` (its
        # `main` is supplied by the libFuzzer runtime at link time), so `swift build --target` only
        # compiles the module and never links the binary. `--product` forces the executable link.
        run: swift build --product AemiJSONFuzz
      - name: Assemble seed corpus from vendored corpora + CTS
        run: |
          mkdir -p corpus
          n=0
          find Tests Benchmarks -type f -name '*.json' 2>/dev/null | while read -r f; do
            cp "$f" "corpus/seed-$n.json" 2>/dev/null || true
            n=$((n + 1))
          done
          if [ -z "$(ls -A corpus 2>/dev/null)" ]; then printf '{"a":[1,2,3],"b":"x"}' > corpus/seed.json; fi
      - name: Run fuzzer (time-boxed)
        env:
          AEMIJSON_FUZZ: "1"
        run: |
          BINDIR="$(swift build --product AemiJSONFuzz --show-bin-path)"
          BIN="$BINDIR/AemiJSONFuzz"
          # Resilient locate: if the binary isn't at the reported bin path, search the build tree.
          if [ ! -x "$BIN" ]; then
            echo "::warning::fuzz binary not at $BIN; searching .build"
            ls -la "$BINDIR" 2>/dev/null || true
            BIN="$(find .build -type f -name AemiJSONFuzz -perm -u+x 2>/dev/null | head -1)"
          fi
          if [ -z "$BIN" ] || [ ! -x "$BIN" ]; then
            echo "::error::AemiJSONFuzz executable not found after build"
            find .build -name 'AemiJSONFuzz*' 2>/dev/null || true
            exit 1
          fi
          echo "Running fuzz binary: $BIN"
          "$BIN" corpus -max_total_time=300 -timeout=10 -rss_limit_mb=2048 -artifact_prefix=crash-
      - name: Upload crash reproducers
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: fuzz-crashes
          path: crash-*
          if-no-files-found: ignore

  benchmarks:
    name: Benchmark (macOS, release)
    needs: quality
    # Benchmarks rebuild the graph in release and are noisy on shared runners, so run them on
    # main / on demand — never on every PR (same gating as the platforms/sanitizers jobs). The
    # run publishes its numbers to the run's job summary for at-a-glance tracking. Regression
    # detection is **advisory** (the threshold step below is `continue-on-error`): hosted-runner
    # variance would make a hard gate flaky, so drift is surfaced in the summary, never failing CI.
    if: github.event_name != 'pull_request'
    runs-on: macos-26
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6
      - name: Select latest Xcode
        run: sudo xcode-select -s "$(find /Applications -maxdepth 1 -name 'Xcode_*.app' -print | sort -V | tail -n 1)"
      - name: Install Swift 6.4 dev toolchain (no hosted macOS image ships 6.4)
        uses: Aemi-Studio/aemi/.github/actions/setup-swift@main
      - name: Show toolchain
        run: swift --version
      - name: Cache SwiftPM
        uses: actions/cache@v5
        with:
          path: |
            .build
            ~/Library/Caches/org.swift.swiftpm
          key: spm-${{ runner.os }}-bench-${{ hashFiles('Package.swift') }}-${{ hashFiles('Sources/**', 'Benchmarks/AemiJSONSuite/**') }}
          restore-keys: |
            spm-${{ runner.os }}-bench-${{ hashFiles('Package.swift') }}-
            spm-${{ runner.os }}-bench-
      - name: Install jemalloc (package-benchmark malloc metrics)
        run: brew install jemalloc
      - name: Fetch conformance & benchmark fixtures
        run: swift package --allow-network-connections all --allow-writing-to-package-directory fetch-fixtures
      - name: Run the benchmark suite (ordo-one/benchmark)
        env:
          AEMIJSON_DEV: "1"
        run: |
          set -o pipefail
          swift package --allow-writing-to-package-directory benchmark --format markdown | tee bench-output.md
      - name: Update & upload benchmark baseline (manual dispatch)
        # The regression gate needs a baseline captured on THIS runner class — a local dev machine
        # miscalibrates hosted-runner timings. Trigger this workflow via "Run workflow" with
        # `update_baseline=true`; download the `benchmark-baseline-main` artifact and commit it under
        # `.benchmarkBaselines/main`. That activates the advisory regression check below; once the runner
        # proves stable, drop that step's `continue-on-error` to make it a hard gate.
        if: github.event_name == 'workflow_dispatch' && inputs.update_baseline
        env:
          AEMIJSON_DEV: "1"
        run: swift package --allow-writing-to-package-directory benchmark baseline update main
      - name: Upload benchmark baseline artifact
        if: github.event_name == 'workflow_dispatch' && inputs.update_baseline
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-baseline-main
          # ordo-one stores baselines at `.benchmarkBaselines/<target>/<name>/`; upload the whole tree
          # (a fresh runner holds only the `main` baseline just generated) for the maintainer to commit.
          path: .benchmarkBaselines/
          if-no-files-found: error
      - name: Regression check vs committed baseline (advisory)
        # Advisory regression gate. When a baseline has been committed under `.benchmarkBaselines/main`
        # — generated on this runner class with one click via the "Update & upload benchmark baseline"
        # step above (manual dispatch, `update_baseline=true`), then committed — this compares the
        # current run against it and reports deltas. It is `continue-on-error` (and a no-op until a
        # baseline exists), so hosted-runner variance can never break the build; it only surfaces drift
        # in the job summary. Promote to a hard gate only once the runner proves stable enough.
        if: always()
        continue-on-error: true
        env:
          AEMIJSON_DEV: "1"
        run: |
          # ordo-one stores baselines at `.benchmarkBaselines/<target>/<name>/`, so probe the glob
          # rather than a fixed `.benchmarkBaselines/main` path (which never matches).
          if ls -d .benchmarkBaselines/*/main >/dev/null 2>&1; then
            {
              echo "## Benchmark regression check (advisory) vs baseline \`main\`"
              echo '```text'
              swift package --allow-writing-to-package-directory benchmark baseline check main 2>&1 || true
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
          else
            echo "_No committed \`main\` baseline (.benchmarkBaselines/*/main); skipping advisory regression check._" \
              >> "$GITHUB_STEP_SUMMARY"
          fi
      - name: AemiJSON-vs-Foundation comparison table
        # Pairs each Foundation baseline with the AemiJSON variant(s) in its workload and reports the
        # speedup — the at-a-glance headline the flat per-benchmark table above doesn't give. The
        # `bench-compare` command plugin reuses the jemalloc-instrumented suite binary the step above
        # already built (so the malloc column is populated; no rebuild), re-running only the comparison
        # workloads standalone to capture the debug histograms it parses.
        env:
          AEMIJSON_DEV: "1"
        run: |
          {
            echo "## AemiJSON vs Foundation (speedup)"
            echo ""
            echo "Release · runner \`${{ runner.os }}\` · commit \`${GITHUB_SHA:0:7}\` · p50 wall-clock, higher speedup = AemiJSON faster"
            echo ""
            swift package bench-compare --filter '(parse|decode|encode|numbers|corpus)/.*'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Publish results to job summary
        if: always()
        run: |
          {
            echo "## AemiJSON benchmarks (ordo-one/benchmark)"
            echo ""
            echo "Release · runner \`${{ runner.os }}\` · commit \`${GITHUB_SHA:0:7}\` · p50/p90/p99 wall-clock + throughput + malloc"
            echo ""
            cat bench-output.md 2>/dev/null || echo "(no benchmark output captured)"
          } >> "$GITHUB_STEP_SUMMARY"

  docs-check:
    name: Build docs (link check)
    needs: quality
    # Build the combined DocC site on every push/PR (no Pages deploy) so a broken `<doc:>` or symbol
    # link fails review instead of slipping to `main`, where the deploy-only `docs` job below is the
    # first to surface it. `--warnings-as-errors` turns unresolved links into a build failure.
    runs-on: macos-26
    env:
      AEMIJSON_DEV: "1"
    steps:
      - uses: actions/checkout@v6
      - name: Select latest Xcode
        run: sudo xcode-select -s "$(find /Applications -maxdepth 1 -name 'Xcode_*.app' -print | sort -V | tail -n 1)"
      - name: Install Swift 6.4 dev toolchain (no hosted macOS image ships 6.4)
        uses: Aemi-Studio/aemi/.github/actions/setup-swift@main
      - name: Cache SwiftPM
        uses: actions/cache@v5
        with:
          path: |
            .build
            ~/Library/Caches/org.swift.swiftpm
          key: spm-${{ runner.os }}-docs-${{ hashFiles('Package.swift') }}-${{ hashFiles('Sources/**') }}
          restore-keys: |
            spm-${{ runner.os }}-docs-${{ hashFiles('Package.swift') }}-
            spm-${{ runner.os }}-docs-
      - name: Build DocC (warnings as errors)
        run: |
          swift package --allow-writing-to-directory "$PWD/_docs_check" \
            generate-documentation \
            --enable-experimental-combined-documentation \
            --target AemiJSONCore --target AemiJSON \
            --disable-indexing --warnings-as-errors \
            --output-path "$PWD/_docs_check"

  docs:
    name: Build & deploy docs (Pages)
    needs: quality
    if: github.ref == 'refs/heads/main'
    runs-on: macos-26
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    # Serialize Pages deploys without cancelling an in-flight publish.
    concurrency:
      group: pages
      cancel-in-progress: false
    env:
      AEMIJSON_DEV: "1"
    steps:
      - uses: actions/checkout@v6
      - name: Select latest Xcode
        run: sudo xcode-select -s "$(find /Applications -maxdepth 1 -name 'Xcode_*.app' -print | sort -V | tail -n 1)"
      - name: Install Swift 6.4 dev toolchain (no hosted macOS image ships 6.4)
        uses: Aemi-Studio/aemi/.github/actions/setup-swift@main
      - name: Cache SwiftPM
        uses: actions/cache@v5
        with:
          path: |
            .build
            ~/Library/Caches/org.swift.swiftpm
          key: spm-${{ runner.os }}-docs-${{ hashFiles('Package.swift') }}-${{ hashFiles('Sources/**') }}
          restore-keys: |
            spm-${{ runner.os }}-docs-${{ hashFiles('Package.swift') }}-
            spm-${{ runner.os }}-docs-
      - name: Build DocC site (static hosting)
        # Combined documentation covers both products — the umbrella `AemiJSON` and the Foundation-free
        # `AemiJSONCore` engine (where `JSON`/`JSONDocument`/`JSONValue`/query types live and are
        # re-exported from). A single-target build can't resolve cross-module symbol links to them.
        run: |
          swift package --allow-writing-to-directory "$PWD/_site" \
            generate-documentation \
            --enable-experimental-combined-documentation \
            --target AemiJSONCore --target AemiJSON \
            --disable-indexing \
            --transform-for-static-hosting \
            --hosting-base-path "${{ github.event.repository.name }}" \
            --output-path "$PWD/_site"
      - name: Add root redirect to the documentation landing page
        run: |
          cat > _site/index.html <<'HTML'
          <!doctype html>
          <meta charset="utf-8">
          <meta http-equiv="refresh" content="0; url=documentation/aemijson/">
          <link rel="canonical" href="documentation/aemijson/">
          <title>AemiJSON</title>
          <a href="documentation/aemijson/">AemiJSON documentation</a>
          HTML
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: _site
      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v5
